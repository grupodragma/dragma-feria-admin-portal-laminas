<style>
    
</style>

<div class="modal-header">
    <h4 class="modal-title">Editar PosicionBanner</h4>
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true"><i class="fas fa-times"></i></span>
    </button>
</div>

<form id="validar_formulario" data-accion="/mantenimiento/guardar-editar-posicionbanner?idposicionbanner=<?php echo $this->idposicionbanner; ?>" data-callback="respuesta(response, 'Posicion Banner actualizado correctamente.')" autocomplete="off">
    <div class="modal-body">
        <div class="form-group">
            <span>
                <label class="form-label">Página</label>
                <select class="select2 form-control" name="idpaginas">
                    <option value="" selected="select" disabled="">Seleccionar...</option>
                    <?php foreach($this->paginas as $item):?>
                    <option value="<?php echo $item['idpaginas'];?>" <?php if( $this->idpaginas == $item['idpaginas'] ) echo "selected='select'"; ?>><?php echo $item['nombre'];?></option>
                    <?php endforeach;?>
                </select>
            </span>
        </div>
        <div class="form-group">
            <label class="form-label">Nombre</label>
            <span>
                <input type="text" name="nombre" class="form-control" value="<?php echo $this->nombre; ?>">
            </span>
        </div>
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-light" data-dismiss="modal">Cerrar</button>
        <button type="button" class="btn btn-info btn-submit btn-uploader-form" data-id-form='validar_formulario'>Guardar Cambios</button>
    </div>
</form>

<script>

    $(".select2").select2({});

    $("#validar_formulario").validate({
        rules: {
            nombre: {
                required: true
            }
        },
        messages: {
            nombre: {
                required: 'Por favor, ingrese el nombre'
            }
        },
        errorPlacement: function (error, element) {
            error.insertAfter(element.parent());
        }
    });

</script>